[package]
name = "malcolm"
version = "0.1.0"
edition = "2024"
description = "Chaos engineering fault injection library"
license = "MIT OR Apache-2.0"
repository = "https://github.com/greysquirr3l/malcom"
readme = "../../README.md"
keywords = ["chaos", "fault-injection", "distributed-systems", "testing", "simulation"]
categories = ["development-tools::testing", "algorithms", "simulation"]

[dependencies]
malcolm-core = { path = "../malcolm-core" }
tracing = { version = "0.1", default-features = false, features = ["std"] }
thiserror = "2"
rand = { version = "0.8", features = ["small_rng"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing-subscriber = { version = "0.3", default-features = false, features = ["registry"] }
chacha20poly1305 = "0.10"
argon2 = "0.5"

[dependencies.tokio]
version = "1"
optional = true
features = ["time", "rt"]

[dev-dependencies]
tracing-test = "0.2"

[features]
default = []
tokio = ["dep:tokio"]
